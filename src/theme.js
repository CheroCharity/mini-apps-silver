// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Blue color (default primary color)
    },
    secondary: {
      main: '#dc004e', // Pink color (default secondary color)
    },
  },
});

export default theme;
